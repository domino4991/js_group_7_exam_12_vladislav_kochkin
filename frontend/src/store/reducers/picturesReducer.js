const initialState = {
    pictures: null,
    picturesError: null,
    picturesLoading: false
};

export const picturesReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};